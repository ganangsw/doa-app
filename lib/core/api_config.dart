class ApiConfig{
  ApiConfig._();

  static const String baseUrl = "https://doa-doa-api-ahmadramadhan.fly.dev";
  static const Duration receiveTimeout = Duration(milliseconds: 15000);
  static const Duration connectionTimeout = Duration(milliseconds: 15000);
}